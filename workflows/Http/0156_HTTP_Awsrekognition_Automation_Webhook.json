{
  "redacted": true,
  "reason": "Removed from the public branch because this workflow contained an embedded API key.",
  "next_steps": "Keep the original workflow only in a private repository or in n8n, and store credentials in n8n credential storage or environment variables. Rotate any exposed keys before reuse."
}
